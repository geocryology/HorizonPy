'''
# Added functionality to adjust contrast of image

# got keybindings to work by using the focus_set() on the main frame when it initializes.  
# Before, the focus was not on the frame, so the keybindings didn't go anywhere.
'''